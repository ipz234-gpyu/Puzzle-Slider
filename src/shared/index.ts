// Shared library exports
export * from './lib';

// Shared types
export type { TileData } from './types';

// Note: UI components should be imported from '@/shared/ui' directly
// to enable tree-shaking and avoid circular dependencies
